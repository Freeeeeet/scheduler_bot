# Scheduler Bot 🤖

Telegram-бот для записи студентов на занятия к учителям.

## 📦 Что внутри

- **Users** - пользователи (студенты и учителя)
- **Subjects** - предметы, которые преподают учителя
- **Schedule Slots** - временные слоты в расписании учителя
- **Bookings** - бронирования занятий

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
go get github.com/pressly/goose/v3
go mod tidy
```

### 2. Настройте PostgreSQL

Создайте базу данных:
```sql
CREATE DATABASE scheduler_bot;
```

### 3. Настройте переменные окружения

Создайте файл `.env` или экспортируйте переменные:

```bash
export DB_DSN="postgres://username:password@localhost:5432/scheduler_bot?sslmode=disable"
export TELEGRAM_TOKEN="ваш_токен_бота"
export ENV="development"
```

### 4. Соберите проект

```bash
go build -o bin/bot ./cmd/bot
```

### 5. Запустите

```bash
./bin/bot
```

При первом запуске автоматически применятся все миграции.

## 🛠 Управление миграциями с goose

### Применить все миграции
```bash
goose -dir migrations postgres "postgres://user:password@localhost:5432/scheduler_bot?sslmode=disable" up
```

### Откатить последнюю миграцию
```bash
goose -dir migrations postgres "postgres://user:password@localhost:5432/scheduler_bot?sslmode=disable" down
```

### Посмотреть статус
```bash
goose -dir migrations postgres "postgres://user:password@localhost:5432/scheduler_bot?sslmode=disable" status
```

### Создать новую миграцию
```bash
goose -dir migrations create название_миграции sql
```

## 📁 Структура проекта

```
scheduler_bot/
├── cmd/bot/          # Точка входа
├── internal/
│   ├── app/          # Логгер, мигратор
│   ├── config/       # Конфигурация
│   ├── controller/   # Telegram handlers и callbacks
│   │   ├── handlers/ # Обработчики команд
│   │   ├── callbacks/ # Обработчики inline кнопок
│   │   └── state/    # Управление состоянием диалогов
│   ├── service/      # Бизнес-логика
│   ├── repository/   # Работа с БД
│   └── model/        # Модели данных
├── migrations/       # SQL миграции (goose)
└── scripts/          # Вспомогательные скрипты
```

## 🎯 Возможности

### Для студентов:
- 📚 Просмотр доступных предметов
- 📅 Запись на занятия
- 📋 Просмотр своих записей
- ❌ Отмена записей

### Для учителей:
- 🎓 Регистрация как учитель
- ➕ Создание и редактирование предметов
- 🗓 Управление расписанием (слоты времени)
- ✅ Одобрение/отклонение записей студентов
- 👥 Просмотр списка учеников

## 🔧 Технологии

- **Go 1.23.4**
- **PostgreSQL** (с pgx драйвером)
- **goose** (миграции)
- **zap** (логирование)
- **viper** (конфигурация)
- **go-telegram-bot** (Telegram API)